---
import ObjectBox from '../module/ObjectBox.astro';
import EmptyCell from './EmptyCell.astro';
import { getTexturePath } from '../../utils/textures.js';

const {
  bot,
  lightWep,
  heavyWep,
  supply,
  cycle,
  shopCards,
  titanWeapon,
  variant = 'standard',
} = Astro.props;
---

<div class="grid-row contents">
  {variant === 'standard' && (
    <>
      {bot ? (
        <>
          <div class="bot-icon-cell">
            {bot.icon_path && <img src={getTexturePath(bot.icon_path, import.meta.env.BASE_URL)} alt="" class="bot-icon" />}
          </div>
          <ObjectBox module={bot.torso[0]} shopCards={shopCards} showName={false} />
          <ObjectBox module={bot.shoulder[0]} shopCards={shopCards} showName={false} />
          <ObjectBox module={bot.chassis[0]} shopCards={shopCards} showName={false} />
        </>
      ) : (
        <>
          <EmptyCell />
          <EmptyCell />
          <EmptyCell />
          <EmptyCell />
        </>
      )}

      <div class="grid-spacer"></div>

      {lightWep ? (
        <ObjectBox module={lightWep} shopCards={shopCards} />
      ) : (
        <EmptyCell />
      )}
      {heavyWep ? (
        <ObjectBox module={heavyWep} shopCards={shopCards} />
      ) : (
        <EmptyCell />
      )}
      {supply ? (
        <ObjectBox module={supply} shopCards={shopCards} />
      ) : (
        <EmptyCell />
      )}
      {cycle ? (
        <ObjectBox module={cycle} shopCards={shopCards} />
      ) : (
        <EmptyCell />
      )}
    </>
  )}

  {variant === 'titan' && (
    <>
      {bot ? (
        <>
          <div class="bot-icon-cell">
            {bot.icon_path && <img src={getTexturePath(bot.icon_path, import.meta.env.BASE_URL)} alt="" class="bot-icon" />}
          </div>
          <ObjectBox module={bot.torso[0]} shopCards={shopCards} showName={false} />
          <ObjectBox module={bot.shoulder[0]} shopCards={shopCards} showName={false} />
          <ObjectBox module={bot.chassis[0]} shopCards={shopCards} showName={false} />
        </>
      ) : (
        <>
          <EmptyCell />
          <EmptyCell />
          <EmptyCell />
          <EmptyCell />
        </>
      )}

      <div class="grid-spacer"></div>

      {titanWeapon ? (
        <ObjectBox
          module={titanWeapon}
          shopCards={shopCards}
        />
      ) : (
        <EmptyCell />
      )}

      <EmptyCell />
      <EmptyCell />
      <EmptyCell />
    </>
  )}
</div>

<style>
  .contents {
    display: contents;
  }
  .bot-icon-cell {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 100%;
    height: 100%;
  }
  .bot-icon {
    width: 100%;
    height: 100%;
    max-height: var(--module-icon-max-height);
    object-fit: contain;
  }
</style>
