name: All

on:
  workflow_dispatch:
    inputs:
      news_url:
        description: War Robots Frontiers news article URL. Leave empty when using items.
        required: false
        type: string
        default: ''
      items:
        description: Plain list of item names to map instead of using scraped article text.
        required: false
        type: string
        default: ''
      target_date_range:
        description: 'Target date range (e.g. "May 26 - June 2"). Leave empty for latest week.'
        required: false
        type: string
        default: ''
      skip_deploy:
        description: Run scrape and map only (skip build/deploy)
        required: false
        type: boolean
        default: false

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  scrape:
    if: ${{ !github.event.inputs.items }}
    runs-on: ubuntu-latest
    env:
      DATA_REPO_PAT: ${{ secrets.DATA_REPO_PAT }}
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Scrape and build game data
        uses: ./.github/actions/scrape
        with:
          news_url: ${{ github.event.inputs.news_url }}

  map:
    runs-on: ubuntu-latest
    needs: scrape
    if: ${{ github.event.inputs.items || needs.scrape.result == 'success' }}
    env:
      GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
      DATA_REPO_PAT: ${{ secrets.DATA_REPO_PAT }}
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Pull scrape commit
        shell: bash
        run: git pull origin "${{ github.ref_name }}"

      - name: Gemini mapping
        uses: ./.github/actions/map
        with:
          target_date_range: ${{ github.event.inputs.target_date_range }}
          items: ${{ github.event.inputs.items }}

  screenshot:
    if: ${{ !inputs.skip_deploy }}
    runs-on: ubuntu-latest
    needs: map
    env:
      DATA_REPO_PAT: ${{ secrets.DATA_REPO_PAT }}
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Pull map commit
        shell: bash
        run: git pull origin "${{ github.ref_name }}"

      - name: Capture discount table screenshot
        uses: ./.github/actions/screenshot

  build:
    if: ${{ !inputs.skip_deploy }}
    runs-on: ubuntu-latest
    needs: screenshot
    env:
      DATA_REPO_PAT: ${{ secrets.DATA_REPO_PAT }}
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Pull map commit
        shell: bash
        run: git pull origin "${{ github.ref_name }}"

      - name: Download table screenshot
        uses: actions/download-artifact@v4
        with:
          name: discount-table
          path: src/frontend/public/

      - name: Build frontend
        uses: ./.github/actions/build

      - name: Upload Pages artifact
        uses: ./.github/actions/deploy

  publish:
    if: ${{ !inputs.skip_deploy }}
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: github-pages
      url: ${{ steps.publish.outputs.page_url }}
    steps:
      - name: Publish to GitHub Pages
        id: publish
        uses: actions/deploy-pages@v4
