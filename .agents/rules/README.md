# Rules

Always-relevant constraints for Windsurf/Cursor (Claude Code does not auto-load
these - it sees a rule only if a skill points at it, or if it is promoted into a
repo-root `CLAUDE.md`).

None yet. This directory exists so the per-tool `rules` symlinks created by
`../setup-symlinks.sh` are not dangling. Add `<name>.md` files here as constraints
emerge; prefer a `skills/` entry when the guidance has a positive trigger.
